name: build-package

on:
  push:
    branches:
      - main
      - master
  pull_request:
  workflow_dispatch:

jobs:

  Build:

    runs-on: ubuntu-latest

    strategy:
      fail-fast: false
      matrix:
        LABVIEW_CONTAINER:
          - 2026q1-linux
          # - 2025q3patch1-linux

    container:
      image: nationalinstruments/labview:${{ matrix.LABVIEW_CONTAINER }}
      options: --user root

    env:
      VIPM_RELEASE_TYPE: preview
      VIPM_VERSION: 26.3.0-3859
      VIPM_COMMUNITY_EDITION: true

    steps:

      # wget is required to download VIPM, and is not included in the base LabVIEW image.
      - name: Install VIPM
        run: |
          apt-get update && apt-get install -y wget
          wget -O /tmp/vipm.deb \
            https://packages.jki.net/vipm/${VIPM_RELEASE_TYPE}/vipm_${VIPM_VERSION}_amd64.deb
          dpkg -i /tmp/vipm.deb && rm /tmp/vipm.deb
          vipm --version

      - name: Install git for actions/checkout                                                                                
        run: apt-get update && apt-get install -y git                                                                         
      - name: Checkout code                                                                                                   
        uses: actions/checkout@v6

      - name: Cache VIPM packages
        uses: actions/cache@v4
        env:
          # Bump to invalidate every VIPM cache (restore-keys included).
          CACHE_VERSION: 1
        with:
          path: /usr/local/jki/vipm/cache/
          key: vipm-v${{ env.CACHE_VERSION }}-${{ runner.os }}-${{ matrix.LABVIEW_CONTAINER }}-${{ env.VIPM_VERSION }}-${{ hashFiles('source/.vipc') }}
          restore-keys: |
            vipm-v${{ env.CACHE_VERSION }}-${{ runner.os }}-${{ matrix.LABVIEW_CONTAINER }}-${{ env.VIPM_VERSION }}-
            vipm-v${{ env.CACHE_VERSION }}-${{ runner.os }}-${{ matrix.LABVIEW_CONTAINER }}-

      - name: Print VIPM About
        run: vipm about

      - name: VIPM Refresh Package Index
        # we will refresh before building the package
        if: true
        run: |
          TARGET_DISPLAY=:99
          export DISPLAY="$TARGET_DISPLAY"
          # Start Xvfb if it is not already running. If it is already running, assume it is correctly configured for this container and do nothing.
          if ! pgrep -x Xvfb > /dev/null; then
              Xvfb "$TARGET_DISPLAY" -screen 0 1280x720x24 -ac +extension GLX +render -noreset \
                  > /tmp/xvfb.log 2>&1 &
          fi
          # Writing this marker file is critical and if if it is not present, the LabVIEW Runtime Engine (required by vipm) may not start properly.
          mkdir -p /tmp/natinst && echo "1" > /tmp/natinst/LVContainer.txt
          # Start Xvfb before the LabVIEW Runtime Engine initializes, which happens on the first `vipm` command.
          echo "$(pgrep -x Xvfb > /dev/null && echo "Xvfb running (DISPLAY=$TARGET_DISPLAY)" || echo "WARNING: Xvfb is required by vipm, but failed to start; DISPLAY=$DISPLAY may not work. Check /tmp/xvfb.log for details.")"
          echo "Starting LabVIEW"
          /usr/local/natinst/LabVIEW-2026-64/labviewpro --headless &

          vipm refresh --show-progress --verbose

      - name: VIPM List Packages
        run: vipm list source/.vipc

      - name: VIPM Install Packages
        # vipc file is empty
        if: true
        run: |
          TARGET_DISPLAY=:99
          export DISPLAY="$TARGET_DISPLAY"
          # Start Xvfb if it is not already running. If it is already running, assume it is correctly configured for this container and do nothing.
          if ! pgrep -x Xvfb > /dev/null; then
              Xvfb "$TARGET_DISPLAY" -screen 0 1280x720x24 -ac +extension GLX +render -noreset \
                  > /tmp/xvfb.log 2>&1 &
          fi
          # Writing this marker file is critical and if if it is not present, the LabVIEW Runtime Engine (required by vipm) may not start properly.
          mkdir -p /tmp/natinst && echo "1" > /tmp/natinst/LVContainer.txt
          # Start Xvfb before the LabVIEW Runtime Engine initializes, which happens on the first `vipm` command.
          echo "$(pgrep -x Xvfb > /dev/null && echo "Xvfb running (DISPLAY=$TARGET_DISPLAY)" || echo "WARNING: Xvfb is required by vipm, but failed to start; DISPLAY=$DISPLAY may not work. Check /tmp/xvfb.log for details.")"
          echo "Starting LabVIEW"
          /usr/local/natinst/LabVIEW-2026-64/labviewpro --headless &

          vipm install oglib_boolean --show-progress --verbose
          # vipm install source/.vipc --show-progress --verbose

      - name: Check user
        run: |
          echo "Current user:"
          whoami
          echo "Home directory:"
          echo "$HOME"
          echo "Contents of home directory:"
          ls -la "$HOME"

      - name: Upload Settings Files as Artifacts
        if: true
        uses: actions/upload-artifact@v7
        with:
          name: vipm-settings-${{ matrix.LABVIEW_CONTAINER }}
          path: |
            /github/home
            /user/local/jki/vipm/Settings.ini
            /root/natinst/.config/LabVIEW-2026/labview*

      - name: VIPM Build Package
        # vipc file is empty
        if: true
        run: |
          TARGET_DISPLAY=:99
          export DISPLAY="$TARGET_DISPLAY"
          # Start Xvfb if it is not already running. If it is already running, assume it is correctly configured for this container and do nothing.
          if ! pgrep -x Xvfb > /dev/null; then
              Xvfb "$TARGET_DISPLAY" -screen 0 1280x720x24 -ac +extension GLX +render -noreset \
                  > /tmp/xvfb.log 2>&1 &
          fi
          # Writing this marker file is critical and if if it is not present, the LabVIEW Runtime Engine (required by vipm) may not start properly.
          mkdir -p /tmp/natinst && echo "1" > /tmp/natinst/LVContainer.txt
          # Start Xvfb before the LabVIEW Runtime Engine initializes, which happens on the first `vipm` command.
          echo "$(pgrep -x Xvfb > /dev/null && echo "Xvfb running (DISPLAY=$TARGET_DISPLAY)" || echo "WARNING: Xvfb is required by vipm, but failed to start; DISPLAY=$DISPLAY may not work. Check /tmp/xvfb.log for details.")"
          echo "Starting LabVIEW"
          /usr/local/natinst/LabVIEW-2026-64/labviewpro --headless &

          vipm refresh --show-progress --verbose
          vipm build source/.vipb --labview-version 2026 --labview-bitness 64 --show-progress --verbose
